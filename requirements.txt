# core
discord.py>=2.2.0,<3.0
aiohttp>=3.8.4

# visual moderation & HTTP
requests>=2.31.0
google-cloud-vision>=2.14.0    # optional: only needed if using Google Vision SafeSearch
python-dotenv>=1.0.0          # optional: load .env files locally

# pin a reasonably modern hashlib/typing backports if needed (usually not required)
